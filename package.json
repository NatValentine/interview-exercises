{
  "name": "interview-exercises",
  "version": "1.0.0",
  "description": "interview exercises compilation",
  "main": "index.js",
  "scripts": {
    "test": "npx tsc -b && npx jest tests.test.js",
    "start": "npx tsc -b && node index.js"
  },
  "author": "Nat Valentine",
  "license": "ISC",
  "dependencies": {
    "typescript": "^5.4.2"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "ts-jest": "^29.1.2"
  }
}
